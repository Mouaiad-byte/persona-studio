{
  "name": "hoard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Operations console for a disclosed AI-persona content studio: generation queue, cross-platform analytics, and honest unit economics.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc -b --noEmit",
    "server": "node server/index.mjs",
    "collect": "node server/cli.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
